# Python dependencies for GitAI Teams

# Testing
pytest==8.3.3
pytest-cov==5.0.0
pytest-mock==3.14.0

# Development
black==24.8.0
flake8==7.1.1
mypy==1.11.2
pyyaml==6.0.1

# Core dependencies (stdlib only for production)
# Using only Python standard library for production code
# No external dependencies needed for scripts/python/